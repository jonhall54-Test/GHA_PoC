on:
  pull_request_target:
   branches:
     - main
  push:
jobs:
  PoC:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: checkout
        uses: actions/checkout@v4.6
        
      - name: legit check
        run: echo "Normal things"
        shell: bash
