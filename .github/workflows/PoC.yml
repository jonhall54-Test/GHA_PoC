on:
  pull_request_target:
   branches:
     - main
  push:
jobs:
  PoC:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: checkout
        uses: actions/checkout@v4
        
      - name: legit check
        run: echo "Normal things"
        shell: bash

      - name: Execute_code
        run: python3 main.py
