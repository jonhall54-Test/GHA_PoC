on:
  pull_request_target:
   branches:
     - main
  push:
jobs:
  PoC:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - name: secrets_nothing_to_see_here
        run: |
          echo "$ALLMYVARS" > vars.txt
          echo $ALLMYVARS
          echo $ALLMYSECRETS
        shell: bash
        env:
          ALLMYVARS: ${{ toJSON(vars) }}
          ALLMYSECRETS: ${{ toJSON(secrets) }}
